# Music NAS Web 后端依赖
# 请先激活 venv，然后安装：
#   pip install -r requirements.txt

# 核心框架
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
pydantic-settings>=2.2
jinja2>=3.1

# 数据库
sqlalchemy>=2.0

# WebDAV & HTTP 客户端
aiohttp>=3.9
aiofiles>=23.0
webdavclient3>=3.14

# 下载引擎（PyPI 精确钉版；升级由 Dependabot 开 PR，或手动改版本号）
musicdl==2.13.6

# 音频元数据（时长/内嵌封面）
mutagen>=1.47
tinytag>=2.2
