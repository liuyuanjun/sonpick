name: Release Docker Image

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build (${{ matrix.suffix }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
            suffix: amd64
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
            suffix: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify version matches tag
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME#v}"
          APP=$(sed -nE 's/^APP_VERSION = "([^"]+)".*/\1/p' app/main.py)
          PKG=$(node -p "require('./web/package.json').version")
          SETUP=$(sed -nE 's/^ *version="([^"]+)".*/\1/p' setup_app.py)
          echo "tag=$TAG app=$APP package=$PKG setup=$SETUP"
          if [ "$TAG" != "$APP" ] || [ "$TAG" != "$PKG" ] || [ "$TAG" != "$SETUP" ]; then
            echo "::error::tag 与 APP_VERSION / package.json / setup_app.py 不一致"
            exit 1
          fi
          echo "VERSION=$TAG" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Login to Aliyun ACR
        uses: docker/login-action@v3
        with:
          registry: registry.cn-beijing.aliyuncs.com
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      - name: Build and push (${{ matrix.platform }})
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: ${{ matrix.platform }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=${{ matrix.suffix }}
          cache-to: type=gha,mode=max,scope=${{ matrix.suffix }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/sonpick:${{ env.VERSION }}-${{ matrix.suffix }}
            ${{ secrets.DOCKER_USERNAME }}/sonpick:${{ env.VERSION }}-${{ matrix.suffix }}
            registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:${{ env.VERSION }}-${{ matrix.suffix }}

  manifest:
    name: Assemble manifests
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - name: Resolve version
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "Owner/repository: ${{ github.repository_owner }} / sonpick"
          echo "Version: $VERSION"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Login to Aliyun ACR
        uses: docker/login-action@v3
        with:
          registry: registry.cn-beijing.aliyuncs.com
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      - name: Assemble multi-arch manifest on GHCR
        run: |
          set -euo pipefail
          VERSION="${{ env.VERSION }}"
          docker buildx imagetools create \
            -t "ghcr.io/${{ github.repository_owner }}/sonpick:${VERSION}" \
            -t "ghcr.io/${{ github.repository_owner }}/sonpick:latest" \
            "ghcr.io/${{ github.repository_owner }}/sonpick:${VERSION}-amd64" \
            "ghcr.io/${{ github.repository_owner }}/sonpick:${VERSION}-arm64"

      - name: Assemble multi-arch manifest on Docker Hub
        run: |
          set -euo pipefail
          VERSION="${{ env.VERSION }}"
          docker buildx imagetools create \
            -t "${{ secrets.DOCKER_USERNAME }}/sonpick:${VERSION}" \
            -t "${{ secrets.DOCKER_USERNAME }}/sonpick:latest" \
            "${{ secrets.DOCKER_USERNAME }}/sonpick:${VERSION}-amd64" \
            "${{ secrets.DOCKER_USERNAME }}/sonpick:${VERSION}-arm64"

      - name: Assemble multi-arch manifest on Aliyun ACR
        run: |
          set -euo pipefail
          VERSION="${{ env.VERSION }}"
          docker buildx imagetools create \
            -t "registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:${VERSION}" \
            -t "registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:latest" \
            "registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:${VERSION}-amd64" \
            "registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:${VERSION}-arm64"

      - name: Inspect GHCR manifest
        run: |
          set -euo pipefail
          docker buildx imagetools inspect \
            "ghcr.io/${{ github.repository_owner }}/sonpick:${{ env.VERSION }}"

      - name: Inspect Aliyun ACR manifest
        run: |
          set -euo pipefail
          docker buildx imagetools inspect \
            "registry.cn-beijing.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/sonpick:${{ env.VERSION }}"

      - name: Note on arch-specific tags
        run: |
          set -euo pipefail
          VERSION="${{ env.VERSION }}"
          echo "Arch-specific tags (${VERSION}-amd64, ${VERSION}-arm64) are kept on all registries for reproducibility."
          echo "Set KEEP_ARCH_TAGS=true to keep them explicitly, or delete them manually if desired."
